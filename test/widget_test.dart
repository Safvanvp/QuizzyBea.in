// Widget tests for QuizzyBea.
// Firebase is not initialized in tests — integration tests should be used for
// Firebase-dependent behaviour.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder – Firebase apps require integration tests',
      (tester) async {
    // Add integration tests in integration_test/ for full app flows.
    expect(true, isTrue);
  });
}
